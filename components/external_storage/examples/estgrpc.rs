// use futures_util::io::{AllowStdIo};

// use external_storage::{
//     GrpcStorage, ExternalStorage
// };

fn main() {
    println!("Start!");
    // let storage = GrpcStorage::new("local", "unix:///tmp/tiri.sock");
    // let data = std::io::Cursor::new("12112".as_bytes().to_vec());

    // storage.write("hello.txt", Box::new(AllowStdIo::new(data)), 2).unwrap();
}
